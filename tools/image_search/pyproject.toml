[project]
name = "image_search"
version = "0.1.0"
description = "Google Custom Search image search with deduplication"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "Pillow>=10.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
